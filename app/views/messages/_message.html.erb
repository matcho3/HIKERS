<li>
  <%= link_to profile_image_for(message.sending_id) %>
  <span class="user">
    <%= link_to message.sending_id.user.name, message.sending_id.user %>
  </span>
  <span class="content"><%= message.content %></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(message.created_at) %> ago.
  </span>
  <%# if current_user?(message.sending_id.user) %>
    <%# link_to "delete", message, method: :delete,
                                     data: { confirm: "You sure?" },
                                     title: message.content %>
  <%# end %>
</li>